# p2p 目录结构说明

> 更新日期：2026-08-17
> 项目：无限大盘（局域网文件传输：电脑端 ⇄ 手机安卓 App，基于 WebRTC 数据通道）
> 当前版本：服务器 v2.13 / 电脑端 v6.18 / 手机端 v5.25（手机端 ≤v5.5 已强制升级拦截）

## 目录总览

```
e:\p2p\
├── p2p-app\              服务器端：Socket.IO 信令服务器 + 浏览器前端
├── p2p_android\          手机端：Flutter 安卓 App 工程（产品名“无限大盘”）
├── p2p_desktop\          电脑端：Flutter Windows 桌面 App 工程（产品名“无限大盘 - 局域网”）
├── p2p_usb_id\           USB 身份认证工具
├── logo\                 图标/品牌素材
├── 免配对码共享连接流程文档.md  免配对码共享连接流程文档（v2.9+ 无限大盘版）
└── 技术设计文档.docx      技术设计文档（Word）
```

---

## 一、p2p-app\ —— 服务器端

| 文件 / 目录 | 作用 |
|---|---|
| `server.js` | **信令服务器主程序**（Socket.IO，端口 3000）。功能：10 位配对码（大写字母+数字）生成/持久化、设备配对与房间管理、WebRTC SDP/ICE 信令转发、托管前端静态页面。v2.7 起增加手机端强制升级拦截（`MIN_ANDROID_VERSION`，旧版激活/连接一律 `APP_VERSION_REQUIRED`）。启动：`node server.js` |
| `index.html` | **电脑端浏览器界面**（单文件）。功能：配对码输入、共享目录选择（File System Access API）、文件浏览、上传/下载、传输进度。使用 simple-peer 建立 WebRTC 数据通道 |
| `package.json` | npm 配置。依赖：express、socket.io；dev 依赖：nodemon。脚本：`npm start` / `npm run dev` |
| `package-lock.json` | npm 依赖版本锁定文件 |
| `.pair-code` | **配对码持久化文件**（10 位大写字母+数字，当前为 `K8A9LE8KKY`），服务器重启后复用，保证配对码稳定 |
| `node_modules\` | npm 依赖包（express/socket.io/nodemon 等），可随时 `npm install` 重建 |

## 二、p2p_android\ —— 手机端（Flutter）

### 2.1 核心代码 `lib\`

| 文件 | 作用 |
|---|---|
| `main.dart` | App 入口。创建全局 `AppController`，配置 Material 3 主题，首页为连接页 |
| `app_controller.dart` | **全局控制器**。状态机（idle/connecting/paired/peerConnected/error/lost）、目录浏览、下载（写文件+进度上报）、上传（分块+背压控制）、传输记录管理 |
| `connect_page.dart` | 连接页。服务器地址（默认 `http://192.168.1.9:3000`）+ 3 位配对码输入，配对成功后跳转主页 |
| `home_page.dart` | 主界面。三个页签：**浏览**（电脑共享目录+下载）、**上传**（选文件发送到电脑）、**传输**（进度/记录），含连接丢失提示 |
| `signaling_service.dart` | 信令客户端（socket_io_client）。与 `server.js` 事件完全兼容：`client:join` / `client:joined` / `client:error` / 信令转发 |
| `rtc_service.dart` | WebRTC 封装（flutter_webrtc）。手机端作为**应答方**（answerer），接收电脑端 offer，处理 ICE candidate，提供数据通道收发（JSON 控制消息 + 二进制文件块） |
| `protocol.dart` | 数据通道协议定义：控制消息 JSON 解析（首字符预筛 + type 白名单，兼容 simple-peer 的 Buffer 行为）、分块大小 64KB、背压阈值 8MB |
| `models.dart` | 数据模型：`FileEntry`（远程文件条目）、`TransferItem`（传输记录，含进度/速度/状态） |

### 2.2 安卓工程 `android\`

| 文件 / 目录 | 作用 |
|---|---|
| `app\build.gradle.kts` | App 构建配置。applicationId `com.p2p.p2p_android`，Java 17，release 版暂用 debug 签名（可安装） |
| `app\src\main\AndroidManifest.xml` | 清单文件。INTERNET / WIFI 权限、`usesCleartextTraffic="true"`（允许访问 http 明文信令地址）、App 名称"无限大盘" |
| `app\src\main\` | 主构建变体：MainActivity、启动主题、图标等 |
| `app\src\debug\` `app\src\profile\` | 调试/性能构建变体资源 |
| `gradle\wrapper\` | Gradle 包装器（gradle-8.14.3，使用腾讯镜像加速下载） |
| `gradlew` / `gradlew.bat` | Gradle 包装器启动脚本（跨平台构建入口） |
| `gradle.properties` | Gradle 全局属性 |
| `local.properties` | 本机 Android SDK 路径（仅本机使用，不提交版本库） |
| `.kotlin\` `sessions\` `errors\` | IDE/构建缓存与错误日志（`errors\` 下是构建失败日志，均可删除，会自动重建） |
| `p2p_android_android.iml` | IDE 模块描述文件 |

### 2.3 其他目录 / 文件

| 文件 / 目录 | 作用 |
|---|---|
| `build\` | **构建产物目录**。APK 输出位置：`build\app\outputs\flutter-apk\`（`app-release.apk` ≈ 102MB、`app-debug.apk` 更大） |
| `pubspec.yaml` | Flutter 项目配置与依赖声明：flutter_webrtc、socket_io_client、path_provider、file_picker、share_plus |
| `pubspec.lock` | 依赖版本锁定文件 |
| `analysis_options.yaml` | 静态分析 / lint 规则配置 |
| `test\` | Flutter 单元测试目录 |
| `.dart_tool\` | Dart 工具缓存（可删，自动重建） |
| `.metadata` | Flutter 工具元数据（记录项目版本信息） |
| `.flutter-plugins-dependencies` | 插件注册元数据（构建自动生成） |
| `README.md` | Flutter 模板默认生成的说明文件 |
| `p2p_android.iml` | IDE 模块描述文件 |

## 三、p2p_desktop\ —— 电脑端（Flutter Windows）

| 文件 | 作用 |
|---|---|
| `main.dart` | App 入口。窗口标题“无限大盘 - 局域网 v6.18”、开机自启检查、托盘逻辑接入 |
| `host_controller.dart` | 全局控制器：共享目录管理、设备配对、WebRTC 连接（P2P 直连/中继）、文件收发、升级检查 |
| `host_service.dart` | WebRTC 主机端封装（answerer 应答）、数据通道控制消息、P2P 限速（16MB/s） |
| `connect_page.dart` | 连接页：生成/展示 10 位配对码二维码（`p2p:<服务器>|<配对码>`） |
| `users_manage_page.dart` | 用户/设备管理：激活码生成（`p2p-act:<码>` 扫码激活） |
| `silent_updater.dart` | 静默升级：下载 zip → MD5 校验 → 解压 → 重启拉起（升级包 `p2p_desktop.zip`） |
| `version.dart` | 版本单一来源（当前 v6.18，与 `server.js` 的 `DESKTOP_VERSION` 保持一致） |
| `windows\runner\main.cpp` | Windows 原生入口：单实例锁、窗口创建、`p2p/window_title` 通道 |
| `windows\runner\tray_icon.cpp` | 系统托盘：托盘提示“无限大盘”、双击恢复、右键退出 |

## 四、根目录文件

| 文件 | 作用 |
|---|---|
| `技术设计文档.docx` | 项目技术设计文档（含架构、信令流程、协议设计等） |
| `~$需求分析.docx` | **Word 打开残留的临时锁文件**（162 字节），原文档关闭后可删除 |

## 五、备注

1. **曾被清理/移动的文件**：早期目录中曾存在 `p2p-verify\`（跨网络 P2P 连通性验证工具）、`需求分析.docx`，**当前均已不在目录中**。若为误删，可从回收站恢复或重新生成。
2. **快速启动**：
   - 服务器端：`cd p2p-app && node server.js`，浏览器打开 `http://localhost:3000`（信令/升级接口）
   - 电脑端：运行 `p2p_desktop\build\windows\x64\runner\Release\p2p_desktop.exe`（或安装升级包）
   - 手机端：安装 `p2p_android\build\app\outputs\flutter-apk\app-release.apk`（v5.8+，仅 arm64-v8a），扫描电脑端二维码或填服务器地址 + 10 位配对码（当前 `K8A9LE8KKY`）。⚠️ 旧版 ≤v5.5 会被强制升级拦截，必须更新后才能使用
3. **可清理项**（不影响功能）：`android\errors\` 日志、`.kotlin\`、`sessions\`、`.dart_tool\`、`node_modules\`（谨慎，删后需 `npm install`）。
