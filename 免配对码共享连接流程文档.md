# 免配对码共享连接 · 流程文档

> 功能目标：用户 B 从未连接过任何电脑，只要电脑端管理员给 B 的手机号创建过共享，B 登录 App 后即可在"共享给我的"中看到该共享并**免配对码**直接连接、浏览、下载/上传。

## 版本信息

| 端 | 版本 | 关键文件 | 状态 |
|---|---|---|---|
| 服务器 | v2.4 | `p2p-app/server.js` | ✅ 已部署（`/opt/p2p-app`） |
| 电脑端 | v5.0 | `host_service.dart` / `host_controller.dart` | ✅ 已构建上传 |
| 手机端 | v4.8 | `share_center_page.dart` / `app_controller.dart` | ✅ 已构建上传 |

---

## 一、系统架构

```mermaid
flowchart LR
    subgraph PC["电脑端 (v5.0)"]
        A1[共享配置<br/>shares.json 持久化]
        A2[hostToken<br/>%APPDATA%/p2p_desktop/host_token]
        A3[host:register 注册上报<br/>+ 共享全量同步]
    end

    subgraph SVR["服务器 (v2.4)"]
        B1[共享注册表 shares.json<br/>token → 设备/配对码/权限/手机号]
        B2[host-tokens.json<br/>deviceId → hostToken 校验]
        B3["POST /api/shares/sync<br/>电脑端上报（需 hostToken）"]
        B4["GET /api/shares/mine<br/>手机端拉取（需登录）"]
        B5["client:join-by-share 信令<br/>按 token 解析配对码"]
    end

    subgraph MOBILE["手机端 (v4.8)"]
        C1["共享给我的 列表页<br/>share_center_page"]
        C2[connectByShare<br/>免配对码连接]
    end

    A1 -- "新增/修改/删除共享" --> A3
    A3 -- "HTTP 同步" --> B3
    B3 -- "全量覆盖写" --> B1
    B3 -- "校验" --> B2

    C1 -- "GET /api/shares/mine" --> B4
    B4 -- "查询 targetPhone 匹配" --> B1
    C2 -- "client:join-by-share" --> B5
    B5 -- "解析 pairCode 后 join" --> PC

    C2 -. "WebRTC P2P 数据通道<br/>(TURN 中转兜底)" .-> A1
```

---

## 二、核心流程

### 2.1 流程一：电脑端共享同步到服务器

```mermaid
sequenceDiagram
    autonumber
    participant U as 电脑端管理员
    participant C as 电脑端 host_controller (v5.0)
    participant S as 服务器 (v2.4)

    U->>C: 创建共享（指定手机号/公开）
    C->>C: 保存到本地 shares.json（持久化）
    C->>C: _syncSharesToServer() 全量上报
    C->>S: host:register 上线注册<br/>携带 deviceId + hostToken
    S->>S: 校验 hostToken 写入 host-tokens.json
    C->>S: POST /api/shares/sync<br/>{deviceId, hostToken, shares[]}
    alt hostToken 不匹配
        S-->>C: 403 拒绝（防伪造）
    else 校验通过
        S->>S: 全量覆盖：先删该设备旧条目<br/>再写入新条目（幂等防残留）
        S-->>C: 200 OK
    end
    Note over S: 注册表每条含：token / deviceId /<br/>pairCode / hostName / name /<br/>folder / perms / targetPhone
```

### 2.2 流程二：手机端拉取"共享给我的"列表

```mermaid
sequenceDiagram
    autonumber
    participant M as 手机端 App (v4.8)
    participant S as 服务器 (v2.4)

    M->>M: 用户登录（AuthService 持有 token）
    M->>M: 打开 AppBar"共享给我的"入口
    M->>S: GET /api/shares/mine<br/>Authorization: Bearer token
    alt 未登录
        S-->>M: 401
    else 已登录
        S->>S: 遍历注册表，筛选 targetPhone == 登录手机号
        S->>S: online = 电脑端 socket 是否在线
        S-->>M: 200 {shares: [...]}<br/>⚠️ 不返回 folder 本地路径
    end
    M->>M: 渲染列表：名称 / 电脑名 /<br/>权限徽标（下载·上传·删除）/ 在线状态
```

### 2.3 流程三：免配对码连接握手（核心）

```mermaid
sequenceDiagram
    autonumber
    participant M as 手机端 (v4.8)
    participant S as 服务器 (v2.4)
    participant H as 电脑端 (v5.0)

    M->>M: 点击在线共享条目
    M->>S: client:join-by-share<br/>{token, deviceName, deviceId, phone, version}
    S->>S: 查共享注册表
    alt token 不存在
        S-->>M: client:error "共享不存在或已失效"
    else token 存在但 targetPhone 不匹配
        S-->>M: client:error "无权访问该共享"
    else 校验通过
        S->>S: 按注册表解析出 pairCode<br/>复用 joinClient 公共流程
        alt 电脑端离线
            S->>S: 登记等待者（host-offline）
            S-->>M: client:error "host-offline"
            M->>M: 提示"电脑端未在线"
        else 电脑端在线
            S->>H: 转发加入请求
            H->>H: 按 token 校验共享与权限
            H-->>S: client:joined（含 TURN 凭证）
            S-->>M: client:joined
            M->>M: 手机端轮询等待数据通道（≤20s）
            M->>H: WebRTC offer/answer + ICE<br/>（直连失败自动 TURN 中转）
            H-->>M: 数据通道 open
            M->>H: user:list（请求身份与共享）
            H-->>M: user:list-result（我的共享列表）
            M->>M: 按 token 匹配共享 → openShare()
            M->>M: ✅ 进入共享浏览页（按 perms 开放操作）
        end
    end
```

### 2.4 流程四：共享生命周期变更

```mermaid
flowchart TD
    A[管理员操作] --> B{变更类型}
    B -->|删除共享| C[removeShare]
    B -->|修改权限| D[updateSharePerms]
    B -->|新增共享| E[createShare]

    C --> F[_persistShares<br/>本地保存 + 服务器同步]
    D --> F
    E --> F

    F --> G{手机端连接中?}
    G -->|是| H[电脑端推送 user:share-updated<br/>手机端自动刷新共享列表]
    G -->|否| I[下次连接时<br/>user:list 下发最新共享]

    H --> J[手机端立即失去访问权<br/>或权限即时生效（电脑端校验）]
```

---

## 三、关键机制说明

| 机制 | 说明 |
|---|---|
| **hostToken 防伪造** | 电脑端首次运行生成 32 位随机 hex（`Random.secure`），落盘 `%APPDATA%/p2p_desktop/host_token`；服务器按 deviceId 记录，sync 接口令牌不匹配返回 403，防止他人冒充电脑端写入共享 |
| **全量覆盖同步** | `POST /api/shares/sync` 每次先删该设备全部旧条目再写入，天然幂等，避免电脑端删除共享后服务器残留幽灵条目 |
| **路径不外泄** | `GET /api/shares/mine` 不返回 `folder` 字段，手机端只能拿到 token/名称/电脑名/权限/在线状态 |
| **权限统一校验** | 连接时电脑端仍按 token 校验共享与权限（与扫码/免配对码同一代码路径）；服务器仅做入口转发 |
| **host-offline 等待登记** | 电脑端离线时服务器登记等待者，电脑端注册上线后补发 `client:joined`（手机端自动重连恢复） |
| **共享持久化** | 电脑端 `Documents/p2p_desktop_logs/shares.json` 落盘，重启恢复后重新同步，保证注册表与本地一致 |
| **配对信息隔离** | 免配对码连接标记 `isShareGuest`，不写入手机端配对历史，界面只开放共享功能 |

## 四、异常场景矩阵

| 场景 | 表现 | 处理 |
|---|---|---|
| 电脑端未开机 | 列表显示"离线"灰色 | 点击提示"该共享的电脑不在线" |
| 电脑端开机但网络抖动 | join 后 15s 无响应 | 手机端超时兜底 → 返回连接失败提示 |
| 共享被删除/电脑端重装 | 服务器无此 token | `client:error "共享不存在或已失效"` → 下拉刷新列表消失 |
| 用户未登录 | AppBar 无"共享给我的"入口 | 登录后常显 |
| 管理员修改权限 | 手机端在浏览中 | 电脑端推送 `user:share-updated`，操作按钮即时按新权限刷新 |
| 服务器重启 | 注册表从 `shares.json` 恢复 | 电脑端 socket 重连后重新注册并全量同步 |

## 五、部署与验证清单

```mermaid
flowchart LR
    A[部署 server.js v2.4<br/>scp + restart.sh] --> B[电脑端替换 v5.0 exe<br/>重启]
    B --> C[手机端安装 v4.8 APK]
    C --> D[验证 /version<br/>→ 2.4 / 5.0 / 4.8]
    D --> E[管理员创建共享<br/>指定用户手机号]
    E --> F[用户登录<br/>打开"共享给我的"]
    F --> G[看到共享 → 点击连接<br/>→ 浏览/下载/上传]
```

**验证命令**

```bash
# 服务器版本
curl http://182.92.157.93:3000/version

# 升级检查
curl "http://182.92.157.93:3000/update-check?platform=android&version=4.7"
curl "http://182.92.157.93:3000/update-check?platform=windows&version=4.9"
```

**排查日志位置**

- 电脑端：`Documents\p2p_desktop_logs\`（含"共享已同步到服务器: N 条"）
- 手机端：App 内"复制运行日志"（含 join-by-share、拉取共享列表、连接握手全过程）
- 服务器：`/tmp/p2p-server.log`（配对日志含版本号与设备标识）
